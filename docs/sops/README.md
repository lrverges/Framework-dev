# Procedimientos Operativos Estandarizados (SOPs)

> [!NOTE]
> La fuente canónica para los SOPs y skills de agentes ahora se encuentra en `ai-specs/agents/`.

Este directorio (`docs/sops/`) puede contener copias sincronizadas por motivos de compatibilidad. Para actualizar las referencias aquí, ejecuta:

```bash
./scripts/sync-ai-specs
```
